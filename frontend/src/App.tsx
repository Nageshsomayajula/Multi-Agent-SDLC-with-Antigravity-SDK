import React from 'react'

export default function App() {
  return (
    <div style={{ padding: 20 }}>
      <h1>Call-attending Agent (PoC)</h1>
      <p>Use the recorder scripts in the <code>tools/</code> folder to join meetings and stream audio to the backend.</p>
      <p>Backend health: <a href="http://localhost:8000">http://localhost:8000</a></p>
    </div>
  )
}
